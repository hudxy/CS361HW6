<html>
    <head>
    </head>
    <body>
        <h1>My Issuses</h1>

        {{#each this}}
        {{#if category}}
        <h3>Issue ID: {{id}}</h3>
        Category: {{category}}<br>
        Urgency: {{urgency}}<br>

        City: {{city}}<br>
        State: {{state}}<br>
        Zipcode: {{zipcode}}<br>
        Description: {{description}}<br>
        {{/if}}
        {{/each}}


    </body>
</html>
